{
  "name": "nitpark-app",
  "version": "1.0.0",
  "description": "NitPark — פלטפורמת החניה הפרטית מס׳ 1 בישראל",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios",
    "cap:build": "npx cap sync && npx cap build android && npx cap build ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "firebase-admin": "^13.10.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
